import { MdlUpgradeDirective } from './mdl-upgrade.directive';

describe('MdlUpgradeDirective', () => {
  it('should create an instance', () => {
    const directive = new MdlUpgradeDirective();
    expect(directive).toBeTruthy();
  });
});
